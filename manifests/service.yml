apiVersion: v1
kind: Service
metadata:
    name: ankitantonydevopscatodolistapp
spec:
    type: LoadBalancer
    ports:
    - port: 8080 
    selector:
        app: ankitantonydevopscatodolistapp